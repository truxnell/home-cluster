---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pyload-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: rook-ceph-block
