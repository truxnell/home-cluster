---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filerun-moya-pv
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: filerun-moya-nfs
  resources:
    requests:
      storage: 1Mi
