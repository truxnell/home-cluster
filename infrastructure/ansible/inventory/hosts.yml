---
all:
  children:
    linux:
      children:
        klipper:
        pikvm:
        roomassistant:
        synchrona:

    storage:
      hosts:
        helios:

    pikvm:
      hosts:
        technocore:

    synchrona:
      hosts:
        synchrona:

    seedbox:
      hosts:
        sdb1:
        sdb2:
