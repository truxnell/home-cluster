---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: leela-database-pv
  labels:
    velero.io/exclude-from-backup: "true"
spec:
  storageClassName: "nfs"
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${SYNOLOGY_IP}
    path: "/volume1/backup/kurbenetes/"
  mountOptions:
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
