---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-tls
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`longhorn.${CLUSTER_DOMAIN}`)
      services:
        - name: longhorn
          port: 80
  tls:
    certResolver: cloudflare
