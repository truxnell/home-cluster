---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-certs-pvc
  namespace: kube-system
spec:
  accessModes:
    - ReadWritemany
  storageClassName: persistent-nas-media
  resources:
    requests:
      storage: 5Mi
