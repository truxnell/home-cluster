---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alertmanager-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-ceph-block
