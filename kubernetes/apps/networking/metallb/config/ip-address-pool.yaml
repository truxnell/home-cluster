---


# yaml-language-server: $schema=https://kubernetes-schemas.trux.dev/metallb.io/ipaddresspool_v1beta1.json
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: bgp-pool
  namespace: networking
spec:
  addresses:
    - ${LB_RANGE}
  autoAssign: true
