---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maloja-config-v1
  namespace: services
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
