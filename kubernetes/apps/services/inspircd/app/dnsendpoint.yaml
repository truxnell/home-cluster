---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared-tunnel-website-domain
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: irc.voltaicforge.com
spec:
  endpoints:
    - dnsName: irc.voltaicforge.com
      recordType: CNAME
      targets: ["games.trux.dev"]
