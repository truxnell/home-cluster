---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: filerun-nfs-nas-media
spec:
  storageClassName: filerun-nas-media
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${SYNOLOGY_IP}
    path: "/volume1/filerun"
  mountOptions:
    - nfsvers=3
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filerun-nfs-nas-media
  namespace: kube-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: filerun-nas-media
  resources:
    requests:
      storage: 1Mi
