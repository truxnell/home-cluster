---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-php-config-v1
  namespace: services
  labels:
    app.kubernetes.io/name: &name nginx-php
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: ceph-block
