apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: trilio.tls
  namespace: trilio
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-production
  secretName: trilio.tls
  dnsNames:
    - trilio.trux.dev
