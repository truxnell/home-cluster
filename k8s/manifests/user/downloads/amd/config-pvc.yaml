---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amd-config
  namespace: downloads
  labels:
    app.kubernetes.io/name: &name amd
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 30Gi

  storageClassName: ceph-block
