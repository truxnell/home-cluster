---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app ecowitt2mqtt
  namespace: home-automation
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 0.1.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  dependsOn:
    - name: mosquitto
      namespace: home-automation
  values:
    image:
      repository: bachya/ecowitt2mqtt
      tag: latest@sha256:2ceb7d08ea0dae0b0bcb354cb576a54ac6fec58ad8def6efbf0c7d912e253d91
    env:
      TZ: "${TZ}"
      ECOWITT2MQTT_MQTT_BROKER: mosquitto.home-automation
      ECOWITT2MQTT_MQTT_PORT: 1883
      ECOWITT2MQTT_PORT: &port 8080
      ECOWITT2MQTT_HASS_DISCOVERY: "true"
    envFrom:
      - secretRef:
          name: *app
    service:
      main:
        ports:
          http:
            port: *port
    ingress:
      main:
        enabled: true
        ingressClassName: "nginx"
        hosts:
          - host: &host "{{ .Release.Name }}.${EXTERNAL_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    # podSecurityContext:
    #   runAsUser: 568
    #   runAsGroup: 568
    #   fsGroup: 568
    #   fsGroupChangePolicy: "OnRootMismatch"
    #   supplementalGroups:
    #     - 44
    #     - 109
    #     - 100
    # resources:
    #   requests:
    #     cpu: 100m
    #     memory: 2000Mi
    #   limits:
    #     memory: 10000Mi
