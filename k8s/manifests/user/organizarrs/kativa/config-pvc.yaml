---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kativa-config-v1
  namespace: organizarrs
  labels:
    app.kubernetes.io/name: &name kativa
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
