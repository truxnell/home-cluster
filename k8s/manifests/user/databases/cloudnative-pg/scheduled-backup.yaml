apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres
  namespace: databases
spec:
  schedule: "0 19 * * *"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: postgres
