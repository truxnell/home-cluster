---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: traefik-ingress
  namespace: network-services
spec:
  endpoints:
    - dnsName: "ipv4.${CLUSTER_DOMAIN}"
      recordType: A
      targets:
        - "${LB_TRAEFIK}"
