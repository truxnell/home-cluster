apiVersion: v1
kind: Service
metadata:
  name: hyperion
  namespace: network-system
spec:
  type: ExternalName

  ports:
    - name: minio
      port: 9000
    - name: http
      port: 5000
  externalName: 10.8.20.10 # fixme syno ip moving
