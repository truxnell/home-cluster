apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: network-system
spec:
  type: ExternalName

  ports:
    - name: https
      port: 9000
    - name: api
      port: 9001

  externalName: hyperion.lab
