---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-php-config-v1
  namespace: service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block
