---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-config-v1
  namespace: services
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi

  storageClassName: ceph-block
