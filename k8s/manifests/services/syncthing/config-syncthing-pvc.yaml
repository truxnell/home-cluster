---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-syncthing-pvc
  namespace: home
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 4Gi
