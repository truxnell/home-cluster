---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mariadb-config-v1
  namespace: databases
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
