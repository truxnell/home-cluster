apiVersion: endpointmonitor.stakater.com/v1alpha1
kind: EndpointMonitor
metadata:
  name: vaultwarden-monitor
  namespace: services
spec:
  forceHttps: true
  urlFrom:
    ingressRef:
      name: vaultwarden
